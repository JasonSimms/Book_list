<div class="container">
    <div class="card">
        <h1>{{book.name}}</h1>
        <h3>By {{book.author}} - Rating: {{book.rating}}</h3>
        <p>{{book.description}}</p>
        <a href="/book-list">Go back Home</a>
    </div>
</div>